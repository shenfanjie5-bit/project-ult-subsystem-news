"""Stage 2.9 canonical smoke tier (per SUBPROJECT_TESTING_STANDARD §2).

Same shape as other modules' smoke tier (audit-eval / contracts /
reasoner-runtime / main-core / data-platform / orchestrator /
entity-registry / subsystem-sdk / subsystem-announcement). Exercise the
5 module-level singletons end-to-end with realistic-but-minimal inputs
and bound the timing for the cheapest paths.
"""
