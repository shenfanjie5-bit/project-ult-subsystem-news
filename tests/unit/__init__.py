"""Stage 2.9 canonical unit tier (per SUBPROJECT_TESTING_STANDARD §2).

Legacy news tests stay in their domain folders (contracts/, signals/,
extract/, normalize/, dedupe/, entities/, sources/, runtime/, graph/) —
iron rule "legacy tests stay in place" applies. New unit tests for the
Stage 2.9 canonical mapper / public.py / version go here.
"""
