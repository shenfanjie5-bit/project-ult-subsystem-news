"""Package version metadata.

0.1.1 — stage 2.9 (milestone-test-baseline): public.py + canonical
test tier dirs + 5 CI lanes + iron rules 1-7 + 3-step Plan A install
(contracts@v0.1.3 -> subsystem-sdk@v0.1.2 -> repo) + canonical wire
mapper in runtime/submit.py (sibling of subsystem-announcement
follow-up #3).
"""

__version__: str = "0.1.1"
